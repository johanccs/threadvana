Review-only. Return `"ok"` from `Solution.PickTool()`.

## Hints
1. Review lesson: `Solution.PickTool()` just returns `"ok"`.
2. No parallelism needed â   a single `return` statement is the whole exercise.
3. Re-read the decision map in the theory if the review questions trip you up.
