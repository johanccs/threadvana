Review-only. Return `"ok"`.

## Hints
1. Review lesson â   `Solution.Run()` only needs `return "ok";`.
2. The real test is the interview Q&A in the front matter; answer them out loud.
3. Compile error? Confirm the method returns `string`.
