Review-only. Return `"ok"`.

## Hints
1. The harness only checks that `Solution.Run()` returns `"ok"` â   the debugging practice happens in the demo and theory.
2. `return "ok";` is the entire solution.
3. Use the interview questions in the front matter to self-test before moving on.
