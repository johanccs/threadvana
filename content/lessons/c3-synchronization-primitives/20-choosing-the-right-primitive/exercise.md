Review-only lesson — read the decision map, run the demo.

## Hints
1. This is a review lesson — the method body only needs to return the expected string: `return "ok";`.
2. Check the method name the harness calls: `Solution.PickPrimitive()`.
3. If it fails to compile, make sure the return type is `string`, not `void`.

Return `"ok"` from `Solution.PickPrimitive()`. The harness just verifies the module loaded.
